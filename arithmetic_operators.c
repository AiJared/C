#include <stdio.h>

int main(){

    // arithmetic operators

    // + (addition)
    // - (subtration)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ (increment)
    // -- (decrement)

    int x = 5;
    int y = 2;

    int z = x + y;

    printf("%d", z);
    return 0;
}